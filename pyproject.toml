[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "prekick"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "numpy",
    "pandas",
    "requests",
    "scipy",
    "streamlit"
]

[dependency-groups]
test = [
    "pytest"
]